The folder 'canonical_protocol' should have an updated version of protocol_controllers, jsPsych, index.html, config.js, etc.

All the available tasks should be part of this protocol. When a new task is created, should be included here after passing the CHECKS.

